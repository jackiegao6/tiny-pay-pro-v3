<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gzc.infrastructure.dao.IGroupBuyOrderListDao">

    <resultMap id="dataMap" type="com.gzc.infrastructure.dao.po.GroupBuyOrderList">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="team_id" property="teamId"/>
        <result column="order_id" property="orderId"/>
        <result column="activity_id" property="activityId"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="goods_id" property="goodsId"/>
        <result column="original_price" property="originalPrice"/>
        <result column="current_price" property="deductionPrice"/>
        <result column="status" property="status"/>
        <result column="out_trade_no" property="outTradeNo"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.gzc.infrastructure.dao.po.GroupBuyOrderList">
        insert into group_buy_order_list(
            user_id, team_id, order_id, activity_id, start_time,
            end_time, goods_id, original_price,
            current_price, status, out_trade_no, create_time, update_time
        )
        values(
                  #{userId}, #{teamId}, #{orderId}, #{activityId}, #{startTime},
                  #{endTime}, #{goodsId}, #{originalPrice},
                  #{currentPrice}, #{status}, #{outTradeNo}, now(), now()
              )
    </insert>

    <select id="queryGroupBuyOrderRecordByOutTradeNo" parameterType="com.gzc.infrastructure.dao.po.GroupBuyOrderList" resultMap="dataMap">
        select user_id, team_id, order_id, activity_id, start_time,
               end_time, goods_id, original_price, current_price, status
        from group_buy_order_list
        where out_trade_no = #{outTradeNo} and user_id = #{userId} and status = 0
    </select>

    <select id="queryOrderCountByActivityId" parameterType="com.gzc.infrastructure.dao.po.GroupBuyOrderList" resultType="java.lang.Integer">
        select count(*)
        from group_buy_order_list
        where activity_id = #{activityId} and user_id = #{userId} and status = 0
    </select>

</mapper>
